tests/WAC.EPD with 20 second moves:
  pass 262, fail 38

tests/WAC.EPD with 5 second moves:

  pass 248, fail 52

tests/WAC.EPD with .25 second moves:

  pass 175, fail 125

Move ordering statistics:

 * We order our best move first around 85% to 90% of the time, and
   second about 5% of the time, then the rest pretty uniformly across
   the tail.

With PVS:

gingell$ sudo nice -20 ./chesley

with PVS:

